name: core

# Cross-platform check of the shared engine (ReLayoutCore): proves the reused
# Swift compiles and passes on both macOS and Windows. Separate from the
# tag-only release workflow.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - name: swift test (engine)
        run: swift test

  test-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install Swift toolchain
        uses: compnerd/gha-setup-swift@397094e75494a93fa8d81db0268dbc8f5d6cf7c6 # main
        with:
          swift-version: swift-6.1-release
          swift-build: 6.1-RELEASE
      - name: swift build (engine + Windows target)
        run: swift build
      - name: swift test (engine)
        run: swift test
