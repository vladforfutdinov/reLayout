# build output (.app/.dmg/.zip produced by scripts/build.sh + make-dmg.sh)
/dist/
# Windows resource compiled by scripts/build-windows.ps1 and CI
/relayout.res

# local owner identity (copy scripts/identity.env.example; CI uses repo variables)
scripts/identity.env
*.o
*.swiftmodule
.sparkle/

# debug
/tmp/relayout.log

# macOS
.DS_Store

# editor local history
.history/
.idea/
.vscode/

# SwiftPM
.build/
docs/SNAPSHOT.md
HANDOFF.md

# per-clone state of the global post-commit history-drift hook
.claude/.last-history-drift-head
