## What's new in v1.3.2 — macOS

- Line breaks survive conversion in VS Code: a triple-clicked line no longer loses its line break, two selected lines are no longer joined into one, and undo with the hotkey no longer eats the line break above.
- In VS Code the hotkey no longer converts the whole line when nothing is selected — it only converts a selection, as everywhere else.
