## What's new in v1.3.5 — Windows (preview)

- The hotkey in VS Code no longer converts a line you never selected. VS Code reports no selection unless its accessibility support is on, and a copy with nothing selected takes the whole line — reLayout now recognises such a copy by the mark the editor itself puts on it.
- With nothing selected, the hotkey converts the word you just typed instead of reaching for the clipboard.
