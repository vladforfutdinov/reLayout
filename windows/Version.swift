// App version shown in the Settings/About section. CI overwrites this file with
// the release tag (or 0.0.0-dev for non-tag builds) before `swift build`; this
// committed default is what local builds compile against.
let appVersion = "dev"
